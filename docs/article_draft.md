<!--
  ドラフト版メモ:
  - Markdownで下書き。公開時はNXPコミュニティブログのHTML規約
    (<li-image>, <li-code>, アンカー見出し, toc-hId- 命名, 目次スムーズスクロール等)
    に合わせて変換する
  - 【 】で囲った箇所は実作業確認後に確定させるプレースホルダ
  - 章番号は0始まり、目次を先頭に配置

  <!-- CLAUDE: ... 
-->

# Zephyrデビュー会 〜FRDM-MCXN947でつまずかず動かす90分〜

## 目次

- [0. この記事は？](#chapter0)
  - [0.1 対象読者](#0-1)
  - [0.2 この記事のゴール](#0-2)
  - [0.3 使うもの](#0-3)
- [1. Zephyrとは？](#chapter1)
  - [1.1 Zephyrの概要と特長](#1-1)
  - [1.2 なぜ今Zephyrなのか](#1-2)
  - [1.3 FRDM-MCXN947について](#1-3)
- [2. 開発環境を触ってみる](#chapter2)
  - [2.1 環境の確認](#2-1)
  - [2.2 HelloWorldサンプルを書き込む](#2-2)
  - [2.3 Blinkyサンプルを書き込む](#2-3)
  - [2.4 サンプルコードの中身をちょっと覗いてみる](#2-4)
- [3. 温度センサーを動かしてみる](#chapter3)
  - [3.1 サンプルをビルド・書き込む](#3-1)
  - [3.2 値を確認する](#3-2)
  - [3.3 なぜ動いたのか？](#3-3)
- [4. HTTP Serverを動かしてみる](#chapter4)
  - [4.1 サンプルをビルド・書き込む](#4-1)
  - [4.2 ブラウザからLEDを制御してみる](#4-2)
  - [4.3 /uptimeエンドポイントを覗いてみる](#4-3)
- [5. HTTP Serverを改変して、温度センサーの値をブラウザで見る](#chapter5)
  - [5.1 何を追加したか](#5-1)
  - [5.2 実際にブラウザで表示してみる](#5-2)
  - [5.3 3章・4章の内容がどう組み合わさっているか](#5-3)
- [6. まとめ](#chapter6)
  - [6.1 今日体験したこと](#6-1)
  - [6.2 自分でもう一度やってみるための情報源](#6-2)
  - [6.3 AIをどう活用できるか](#6-3)

---

<a id="chapter0"></a>
## 0. この記事は？

この記事は、ハンズオン・セミナー「Zephyrデビュー会」の進行ガイドとして書かれたものです。当日の会場では、この記事の手順に沿って一緒に手を動かしていきます。同時に、都合が合わず当日参加できなかった方にも、この記事だけを読みながら同じ内容を自分の環境で再現できるよう、コマンド例や確認ポイントを省略せずに書き下しています。読み物としての解説記事というより、実際に「動くところまで」たどり着くための作業ガイドだと捉えてください。

<a id="0-1"></a>
### 0.1 対象読者

「Zephyrって聞いたことはあるけど触ったことはない」というZephyr OS初心者、これからZephyrを触ってみたい方に向けた記事です。組み込み開発の経験は問いません。C言語のコードを読んだことがある程度で十分です。

<a id="0-2"></a>
### 0.2 この記事のゴール

NXPのFRDM-MCXN947基板を使って、LEDの点滅から温度センサーの読み取り、さらにブラウザ経由でセンサー値を表示するところまでを、実際に手を動かして体験します。ゴールは「Zephyrで簡単にアプリが動かせる」という成功体験を持ち帰ってもらうことです。難しい設定やコーディングは、この記事の中では最小限に留めます。

<a id="0-3"></a>
### 0.3 使うもの

- ハードウェア
  - FRDM-MCXN947評価ボード
  - PC（Zephyr開発環境など下記ソフトウェアをインストールしておく）
  - 温度センサ: P3T1755 I3C/I²Cバス対応、±0.5°C精度デジタル温度センサ (FRDM-MCXN947評価ボードに搭載)
  - Ethernetケーブル

- ソフトウェア(インストール，セットアップ済みであること)
  - Zephyr開発環境
  - Microsoft Visual Studio Code
  - MCUXpresso for VSC
  - ターミナルソフト(Teraterm，CoolTermなど)
  - webブラウザ

【開発ツールのインストール方法を紹介したブログ記事へのリンクを挿入】

---

<a id="chapter1"></a>
## 1. Zephyrとは？

<a id="1-1"></a>
### 1.1 Zephyrの概要と特長

Zephyrは、Linux Foundation配下で開発が進むオープンソースのRTOS（リアルタイムOS）です。小規模なマイコンから比較的リソースの大きなSoCまで、幅広いハードウェアを対象にしています。対象範囲の広さはNXP製品だけを見ても分かりやすく、今回使うMCXシリーズのようなCortex-Mコアのマイコンから、i.MXシリーズのようなCortex-Aプロセッサを使うアプリケーションプロセッサまで、同じZephyrという1つのOSでカバーしています。マイコンからプロセッサまでを1つのRTOSで扱えるという点は、ボードごとにOSやビルド環境を使い分けなくて済むという実用上のメリットに直結します。

特長を一言でまとめると、「デバイスツリー」と「Kconfig」という2つの仕組みで、ハードウェア構成とソフトウェア機能の有効/無効を分離して管理できる点です。この分離のおかげで、同じアプリケーションコードを複数のボードに展開しやすくなっています。

言い換えると、C言語などで書くアプリケーションのロジックそのものは「何のセンサーが」「どのバスに」つながっているかを直接知らなくてよい、という設計になっています。ハードウェア構成はデバイスツリー、機能のオン/オフはKconfigというソフトウェア・モジュール側の設定に閉じ込められていて、アプリケーションのコードは`DEVICE_DT_GET`のようなAPIを通してデバイスにアクセスするだけです。この「アプリのロジック」と「ハードウェア・ソフトウェアモジュールの構成」の分離を、3章で実際のコードを見ながら体感します。

<a id="1-2"></a>
### 1.2 なぜ今Zephyrなのか

半導体メーカー各社が、既存製品ラインを含めZephyrへの対応を広げています。特定のベンダーに縛られないRTOSとして、業界内での存在感が増しているのが現状です。

Zephyrプロジェクト自体は2016年にLinux Foundation配下でスタートしており、今日でおよそ10年の歴史を持ちます。決して新しくて枯れていない技術ではなく、この間に積み重ねられてきたのは、CPUアーキテクチャやペリフェラルドライバだけではありません。今回4章・5章で体験するHTTP ServerのようなTCP/IPスタックやHTTPサーバーといったネットワーク関連のミドルウェアも、Zephyr本体に標準搭載されたサブシステムとして育ってきています。マイコン向けOSというと最小限のスケジューラ機能だけを想像しがちですが、こうした上位のミドルウェアまで一貫してKconfigで有効化できる点も、Zephyrを選ぶ理由のひとつです。

「結局何がそんなに良いのか」は、触ってみないと実感しにくい部分です。この記事では、実際に手を動かしながらその感触を確かめていきます。

<a id="1-3"></a>
### 1.3 FRDM-MCXN947について

FRDM-MCXN947は、NXPのMCX N947マイコンを搭載した評価ボードです。150MHzで動作するCortex-M33のデュアルコア構成で、GPIO・I2C・I3C・SPI・Ethernetなど様々なペリフェラルをArduinoヘッダ，MikroBusソケットほか様々なコネクタ経由で扱えます。

【ボード写真を挿入】

---

<a id="chapter2"></a>
## 2. 開発環境を触ってみる

<a id="2-1"></a>
### 2.1 環境の確認

会場のPCには、west・Zephyr SDKを含む開発環境をあらかじめセットアップしてあります。皆さんに環境構築で手間取っていただく必要はありません。まずはターミナルを開いて、以下のコマンドでwestが使える状態になっていることを確認します。

<!-- CLAUDE: 
「帰ってから，自身の環境をセットアップするには」という情報を入れる．参考情報は
公式ガイド→ https://docs.zephyrproject.org/latest/develop/getting_started/index.html
楽チン環境設定(NXPツール使用)→ https://community.nxp.com/t5/NXP-Tech-Blog/Zephyr-シリーズ-第２回-はじめてのビルドと実機動作-日本語ブログ/ba-p/2189007
-->

<!-- CLAUDE: 
この受講者のPCには公式ガイドに沿ったZephyr開発ツールがインストールされていることを述べる．
Zephyrツールは基本的にコマンドラインで使うようになっている．
さらにMCUXpresso for VSCも使えるようになっているので，どちらでも使える．
しかしこのセッションではあえてコマンドラインを使った体験をしてもらう．チップベンダに依らない，どのターゲットでも同じ使い方ができる方法で体験してもらうためということを書く
-->


【どのターミナルを使うか．操作方法も示しておく】

westコマンドを使う前に、Zephyr用のPython仮想環境と環境変数を読み込んでおく必要があります。以下の2行を実行してから`west --version`を叩いてください。

<!-- CLAUDE: 
「叩く」という言葉は使わない．少しだけフォーマルな言い方に
-->

```
source ~/zephyrproject/.venv/bin/activate
source ~/zephyrproject/zephyr/zephyr-env.sh
west --version
```

<!-- CLAUDE: 
この記事の全てのコマンドには各コマンドとそのオプション指定の解説を用意しておく．本文の流れを乱さないようにquoteやコラム形式などの別枠内に書く
-->

このターミナルはセミナーの間は開いたままにしておいてください。
この後の章でも使う`west`コマンドの実行に必要です．。閉じてしまうと環境変数の読み込みをし直さなければなりません．

【ユーザのために，再度入力が必要になったときに楽ができるよう，シェルスクリプトとコマンドエイリアスを仕込んでおく】

【実行例のスクリーンショットを挿入】

<a id="2-2"></a>
### 2.2 HelloWorldサンプルを実行してみる

これ以降、サンプルコードは`zephyr/samples/`配下のものを直接編集するのではなく、受講者ごとの作業用フォルダにコピーしてから、そのコピーに対してビルド・書き込みを行います。`zephyr/samples/`配下は元のサンプル集として手を加えずに残しておき、実験や書き換えは自分のフォルダの中だけで完結させる、という進め方です。

まずZephyrに標準で含まれる`hello_world`サンプルコードを以下のコマンドでコピーし，そのディレクトリに移動します．

```
cp -r samples/hello_world ~/handson/hello_world
cd ~/handson/hello_world
```

このフォルダ内で`hello_world`をビルドします．

```
west build -p auto -b frdm_mcxn947/mcxn947/cpu0 .
```

<!-- CLAUDE: 
コマンドとオプションの解説の中に次の内容を書いておく．
「-b」オプションに続く引数はターゲット基板の指定であること．
FRDM-MCXN947はデュアルコアのマイコンなのでこのような指定が必要であること．
FRDM-MCXA153のような基板ではfrdm_mcxa153だけで済むこと．
「frdm_mcxn947/mcxn947/cpu0」は簡略化して「frdm_mcxn947//cpu0」と書くこともできること．簡略化できる理由も書いておく．
「-p」も何のためにあるのかを明記．pristineについては次節で詳しく．
-->


ビルドが完了すると，次のようなビルドの結果情報が表示されます．
エラーの場合はその情報が表示されます．ただし，Zephyrのエラー情報は大量になることが多く，最初はそれを追うのが大変です．エラーメッセージ注意深く読んだり，あるいはAIの力を借りて把握するのが良いでしょう．

ビルドの結果，ターゲットのマイコンに書き込むコードができました．以下のコマンドで書き込みます．

<!-- CLAUDE: 
ビルドで生成されたものはどこにあるのかを別枠で解説しておく．「--pristine」オプションについても触れておく．
-->


```
west flash
```

書き込みが終わるとマイコンがリセットされ，書き込んだコードが実行されます．

<!-- CLAUDE: 
ここではまだターミナルしか開いてないので，シリアルコンソールを開く指示を入れる．シリアルコンソールにはCoolTermを使う．ポートの指定やボーレート設定の方法も書いておく．
-->

シリアルコンソールに`Hello World! frdm_mcxn947/mcxn947/cpu0`のような文字列が表示されれば成功です。

【実行例のスクリーンショットを挿入】

`hello_world`の`src/main.c`は、次のように驚くほど短いコードです。
<!-- CLAUDE: 
そのファイルを受講生のPCで開く方法を書いておく
-->

```c
#include <stdio.h>

int main(void)
{
	printf("Hello World! %s\n", CONFIG_BOARD_TARGET);

	return 0;
}
```

<!-- CLAUDE: 
これをみる限り至ってフツーのC言語によるHello worldコードであることを強調．強いて言えば「`CONFIG_BOARD_TARGET`マクロがこの環境で宣言されているんだろうな」ぐらいのコード．マイコンの初期化としてよくあるクロックの設定やペリフェラルの初期化はこのコードにはないことを述べる
-->

<!-- CLAUDE: 
次の節はRTOSのコードとしての，よくある例との比較になっているので，「マイコンのコードっぽくない」ことに加えて「RTOSのコードっぽくない」ことも述べておく
-->

タスクの生成もスケジューラの起動宣言も無く、`main()`から`printf()`を呼ぶだけの、PC向けのC言語プログラムとほとんど同じ見た目です。多くのRTOSでは「Hello World」のために、タスク（スレッド）を明示的に作って`osKernelStart()`のような呼び出しでスケジューラを起動する、という手順が必要になりますが、Zephyrでは`main()`自体がすでに1つのスレッドとして動いており、その手前の初期化はビルドシステムとKconfigの設定に任されています。この「素のCプログラムに近い書き味」は、Zephyrがアプリケーション開発者に見せる顔の特徴のひとつです。

<!-- CLAUDE: 
main.c以外に必要なファイルについても説明しておく．最低限のプロジェクトにはprj.confとCMakeLists.txtだけあればいい？
-->


<a id="2-3"></a>
### 2.3 Blinkyサンプルを書き込む

続けて`samples/basic/blinky`を書き込みます。手順はHelloWorldと同じで、まず作業用フォルダにコピーしてから、ビルド対象のパスだけが変わります。

<!-- CLAUDE: 
これ以降のターゲット指定は簡略化した形式=「frdm_mcxn947//cpu0」を使う
-->


<!-- CLAUDE: 
************************************ チェックとコメント追加はここまで 2026-08-17
-->


```
cp -r samples/basic/blinky ~/handson/blinky
cd ~/handson/blinky
west build -p auto -b frdm_mcxn947/mcxn947/cpu0 .
west flash
```

ボード上のLEDが点滅すれば成功です。

<a id="2-4"></a>
### 2.4 サンプルコードの中身をちょっと覗いてみる

`blinky`の`src/main.c`を開くと、GPIOをトグルするだけの短いコードになっていることがわかります。あわせて`boards/`フォルダの中にあるデバイスツリーオーバーレイ（`*.overlay`）にも軽く触れておきます。ここで出てくる「デバイスツリー」というキーワードは、3章で温度センサーの動作原理を説明する際にもう一度登場します。

---

<a id="chapter3"></a>
## 3. 温度センサーを動かしてみる

<a id="3-1"></a>
### 3.1 サンプルをビルド・書き込む

`samples/sensor/thermometer`を、2章と同じように作業用フォルダにコピーしてからビルド・書き込みします。

```
cp -r samples/sensor/thermometer ~/handson/thermometer
cd ~/handson/thermometer
west build -p auto -b frdm_mcxn947/mcxn947/cpu0 .
west flash
```

<a id="3-2"></a>
### 3.2 値を確認する

シリアルコンソールに、1秒ごとに`Temperature is 24.3°C`のような形式で気温の値が周期的に表示されます。

【実行例のスクリーンショットを挿入】

<a id="3-3"></a>
### 3.3 なぜ動いたのか？

センサーの値がなぜ表示されたのか、`src/main.c`と`boards/*.overlay`を見ながら種明かしをします。

`main.c`側では、センサーデバイスを次のように取得しています。

```c
static const struct device *const temp_dev = DEVICE_DT_GET(DT_ALIAS(ambient_temp0));
```

この`ambient_temp0`というエイリアスが、実際にどのハードウェア（センサー品種・接続バス）を指すのかを決めているのが`boards/*.overlay`です。ここで初めて「デバイスツリー」という言葉が実感を伴って登場します。今回のFRDM-MCXN947では、温度センサーはI3Cバスにぶら下がっており、`boards/frdm_mcxn947_mcxn947_cpu0.overlay`には次のように定義されています。

```dts
/ {
	aliases {
		ambient-temp0 = &p3t1755;
	};
};

&i3c1 {
	status = "okay";

	p3t1755: p3t1755@4800000236152a0090 {
		compatible = "nxp,p3t1755";
		reg = <0x48 0x0236 0x152a0090>;
		status = "okay";
	};
};
```

`aliases`ブロックで`ambient-temp0`を`p3t1755`ノードに結び付け、`i3c1`バス配下に`nxp,p3t1755`ドライバを持つセンサーノードを1つ生やしているだけです。`main.c`側は`DT_ALIAS(ambient_temp0)`としか書いていないので、このセンサーがI2Cではなく（より高速な）I3Cで繋がっていることも、レジスタアドレスの詳細も、アプリケーションコードは一切知りません。

コードを一行も書かずに、「デバイスツリーの定義を変えるだけで別のハードウェア構成に対応できる」というのがここでの見どころです。たとえば同じセンサーをI2C接続のボードに載せ替えたり、別の温度センサーIC（対応ドライバがある前提）に変更したりする場合でも、変更するのは基本的に`boards/*.overlay`の中だけで、`main.c`の`DEVICE_DT_GET(DT_ALIAS(ambient_temp0))`という1行はそのまま使い回せます。

> **コラム: 一般的な組み込み開発との違い**
> OSを使わないベアメタル開発や、デバイスツリーの仕組みを持たない多くのRTOSでは、センサーのI2C/I3Cアドレスやレジスタ操作をアプリケーションコードの中に直書きするのが一般的です。そのため、センサーの型番やバス配線が変わるたびに、アプリケーションロジックの書き換えとリビルドが必要になります。Zephyrでは、この「どの部品が」「どこに」繋がっているかという情報をデバイスツリーという別レイヤーに追い出しているため、ボードが変わってもアプリケーションのロジック（この場合は「気温を読んで表示する」という処理）そのものは触らずに済みます。今回のセミナーでは時間の都合でここまで深入りしませんが、興味を持った方はぜひ他のボード向け`overlay`ファイルも見比べてみてください。同じ`samples/sensor/thermometer`でも、ボードごとにハードウェア接続だけが違う`overlay`が用意されていることが分かるはずです。

---

<a id="chapter4"></a>
## 4. HTTP Serverを動かしてみる

FRDM-MCXN947でHTTP Serverサンプルを動かす手順自体は、単発のブログ記事としてすでに公開されています。PC側のネットワーク設定や、固定IPの代わりにDHCPを使う場合の手順など、より詳しい内容はそちらも参照してください。

- [Zephyr HTTP ServerをFRDM-MCXN947で動かしてみた（NXP Tech Blog、日本語）](https://community.nxp.com/t5/NXP-Tech-Blog/Zephyr-HTTP-Server%E3%82%92FRDM-MCXN947%E3%81%A7%E5%8B%95%E3%81%8B%E3%81%97%E3%81%A6%E3%81%BF%E3%81%9F-%E6%97%A5%E6%9C%AC%E8%AA%9E%E3%83%96%E3%83%AD%E3%82%B0/ba-p/2379867)

<a id="4-1"></a>
### 4.1 サンプルをビルド・書き込む

`samples/net/sockets/http_server`をビルド・書き込みします。今回はTLSを使わないプレーンHTTP・固定IP接続で進めます。

これも作業用フォルダにコピーしてから、`prj.conf`の末尾に以下の3行を追記してIPアドレスを固定します。

```
cp -r samples/net/sockets/http_server ~/handson/http_server
cd ~/handson/http_server
```

```
CONFIG_NET_CONFIG_MY_IPV4_ADDR="192.168.1.100"
CONFIG_NET_CONFIG_MY_IPV4_GW="192.168.1.1"
CONFIG_NET_CONFIG_MY_IPV4_NETMASK="255.255.255.0"
```

Gateway（`CONFIG_NET_CONFIG_MY_IPV4_GW`）は、ルーターを経由しない通信では本来必須ではありません。今回はPCとボードをEthernetケーブルで直結し、同じ`192.168.1.0/24`のサブネットに両方を置くので、通信はすべて同一リンク内のARPで解決され、Gatewayを経由するルーティングは発生しないためです。設定自体はサンプルの流儀に合わせて残していますが、「直結構成では実質使われない設定である」という点を押さえておいてください。

```
west build -p auto -b frdm_mcxn947/mcxn947/cpu0 .
west flash
```

PCとボードをEthernetケーブルで直結し、あらかじめ設定済みの固定IPで通信します。

<a id="4-2"></a>
### 4.2 ブラウザからLEDを制御してみる

ブラウザで`http://<ボードのIPアドレス>/`にアクセスすると、LED制御用のページが表示されます。ボタン操作でボード上のLEDが点灯/消灯することを確認します。

【画面キャプチャを挿入】

> **コラム: ネットワーク機能の「重さ」について**
> 一般的な組み込み開発では、HTTPサーバーを1つ動かすだけでも、TCP/IPスタック（lwIPなど）の組み込み・設定、ソケットAPIの用意、HTTPパーサーの選定・実装、静的ファイルの配信方法の実装、と多くの下準備が必要になりがちです。今回はそれらがすべて`prj.conf`の`CONFIG_NET_*`・`CONFIG_HTTP_*`系オプションを`y`にするだけで揃っており、アプリケーション側で書いたのは各エンドポイントのハンドラ関数だけです。ネットワークスタックからHTTPサーバーまでを1つのビルドシステムとKconfigで一貫して扱えるのは、Zephyrがマイコン向けRTOSでありながら比較的大きなミドルウェア込みで語られる理由のひとつです。

<a id="4-3"></a>
### 4.3 /uptimeエンドポイントを覗いてみる

`http://<ボードのIPアドレス>/uptime`にアクセスすると、ボードの起動からの経過時間が返ってきます。この「GETリクエストに対して、値を計算してテキストで返す」という一番シンプルなエンドポイントの作りを、次の5章でそのまま応用します。

---

<a id="chapter5"></a>
## 5. HTTP Serverを改変して、温度センサーの値をブラウザで見る

<a id="5-1"></a>
### 5.1 何を追加したか

4.3で見た`/uptime`エンドポイントと同じ作りで、`/temperature`エンドポイントを追加しました。変更は`src/main.c`のごく一部です。

```c
static int temperature_handler(struct http_client_ctx *client, enum http_transaction_status status,
                const struct http_request_ctx *request_ctx,
                struct http_response_ctx *response_ctx, void *user_data)
{
    /* 3章の温度センサーと同じ device_get / sensor_sample_fetch_chan /
       sensor_channel_get の流れで気温を取得し、HTMLとして返す */
    ...
}
```

追加したのは、この`temperature_handler`と、それを`/temperature`というパスに紐づける1行だけです。他の部分（LED制御、`/uptime`、静的ページ配信）は元のサンプルのまま変更していません。

<a id="5-2"></a>
### 5.2 実際にブラウザで表示してみる

ブラウザで`http://<ボードのIPアドレス>/temperature`にアクセスすると、その時点の気温がHTMLで表示されます。

【画面キャプチャを挿入：Temperature: XX.XXX°C】

<a id="5-3"></a>
### 5.3 3章・4章の内容がどう組み合わさっているか

3章で動かした温度センサーの読み取りコードと、4章で動かしたHTTP Serverの仕組みを組み合わせただけ、というのがこのステップの種明かしです。それぞれ単体で理解した部品を組み合わせると、ブラウザからセンサー値を見るというアプリケーションになる、という流れを振り返ります。

> **コラム: main.cとCMakeLists.txtが少し煩雑に見える理由**
> 実際に`src/main.c`を開くと、`/temperature`ハンドラ以外にもかなりの行数があることに気づきます。これは、このHTTP Serverサンプルが「Webサーバーとして配信するデータ一式（トップページのHTML、JavaScript、LED制御・uptime・ネットワーク統計といった元からある複数のエンドポイント）」もあわせて用意しているためです。特に、`index.html`や`main.js`は`CMakeLists.txt`内の`generate_inc_file_for_target()`によってビルド時にgzip圧縮され、`index.html.gz.inc`のようなCヘッダとして`main.c`から`#include`される形で埋め込まれています。また、`HTTP_RESOURCE_DEFINE()`で登録した各エンドポイントは、`sections-rom.ld`というリンカスクリプトが用意する専用のセクション（`http_resource_desc_test_http_service`など）に配置されることで、HTTPサーバーが起動時にエンドポイント一覧を辿れるようになっています。この`sections-rom.ld`と`CMakeLists.txt`側の`zephyr_linker_sources()`の対応が崩れると、`_http_resource_desc_test_http_service_list_start`のようなシンボルが見つからずリンクエラーになります。これらは温度センサーの追加そのものとは直接関係のない「HTTPサーバーを名乗るために必要な足回り」なので、今回はそういうものだと割り切って、`temperature_handler`と`temperature_json_handler`の中身だけに注目してもらえれば十分です。

---

<a id="chapter6"></a>
## 6. まとめ

<a id="6-1"></a>
### 6.1 今日体験したこと

- Zephyrの開発環境（west・デバイスツリー・Kconfig）に触れた
- Lチカ、温度センサー読み取り、HTTP Serverをそれぞれ動かした
- 2つのサンプルを組み合わせて、ブラウザでセンサー値を見るアプリケーションを作った

<a id="6-2"></a>
### 6.2 自分でもう一度やってみるための情報源

- [Zephyr Project公式ドキュメント](https://docs.zephyrproject.org/latest/)
- [今回使ったサンプルコードのリポジトリ (teddokano/handson_http_server_temp)](https://github.com/teddokano/handson_http_server_temp)
- 【この記事自体へのリンクを挿入(公開後に確定)】

<a id="6-3"></a>
### 6.3 AIをどう活用できるか

Zephyrのサンプルコードを読む・改変する際、AIアシスタントに「このサンプルにこういう機能を足すには」と相談すると、既存コードのパターンに沿った改変案を提示してもらえます。今回の`/temperature`エンドポイントも、既存の`/uptime`エンドポイントと同じ構造で追加しています。わからない箇所があれば、まず既存のサンプルコードの近い部分を探し、AIと一緒に読み解いてみることをおすすめします。

もう一つよくあるのが、`west build`でエラーになったときです。Zephyrのビルドエラーは、Kconfigの警告からCMakeのエラー、リンカのエラーまで一度に大量のメッセージが出力され、どこが本質的な原因なのか初見では分かりにくいことがあります。そんなときも、エラーメッセージ全体をそのままAIに貼り付けて「これはどういうエラーですか」と聞いてみるところから始めるのがおすすめです。本記事の4〜5章で登場した`sections-rom.ld`まわりのリンクエラーも、実際にはこの記事を書く過程でAIと一緒に原因を特定したものです。